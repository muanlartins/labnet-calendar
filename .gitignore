node_modules/
.DS_Store
*.log
.env
.env.*

# Build artifacts — regenerated by `npm run all` (locally) or CI on every push.
# Excluded from git so refresh PRs only diff source data, not the compiled output.
web/data/events.json
web/data/*.ics
